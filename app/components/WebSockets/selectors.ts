export const getWebsocket = (state) => state.websocket.instance;

export const getIdToken = (state) => state.websocket.idToken;

export const getIsLoading = (state) => state.websocket.isLoading;

export const getIsConnected = (state) => state.websocket.isConnected;

export const getError = (state) => state.websocket.error;

export const getData = (state) => state.websocket.data;
