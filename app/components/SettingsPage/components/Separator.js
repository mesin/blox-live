import styled from 'styled-components';

const Separator = styled.div`
  width: 100%;
  height: 1px;
  background-color: ${(props) => props.theme.gray80015};
  margin-bottom: 35px;
`;

export default Separator;
