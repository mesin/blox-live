import React from 'react';
import styled from 'styled-components';
import SubTitle from '../../SubTitle';

const Wrapper = styled.div``;

const TwoFactor = () => (
  <Wrapper>
    <SubTitle>Two-step verification</SubTitle>
  </Wrapper>
);

export default TwoFactor;
