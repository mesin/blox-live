export const getIsLoading = (state) => state.login.isLoading;

export const getIsLoggedIn = (state) => state.login.isLoggedIn;

export const getIdToken = (state) => state.login.idToken;

export const getUserData = (state) => state.login.userData;

export const getError = (state) => state.login.error;
